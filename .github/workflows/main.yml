name: Deploy Prod Backend (PM2)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            set -e
            if [ ! -d "/root/fleet/fleet-backend" ]; then
              mkdir -p /root/fleet
              git clone https://github.com/anjalimaitrii/logistic-backend.git /root/fleet/fleet-backend
            fi
            cd /root/fleet/fleet-backend
            git pull origin main
            npm ci
            npx tsc
            pm2 reload ecosystem.config.cjs --env production 2>/dev/null || pm2 start ecosystem.config.cjs --env production
            pm2 save
            echo "✅ fleet-backend deployed via PM2"
